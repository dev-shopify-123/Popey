<script
  type="application/json"
  data-section-id="{{ section.id }}"
  data-section-type="dynamic-html"
></script>

<section class="custom-html--container">
  {% if section.settings.title != blank %}
    <h2 class="home-section--title" >
      <span data-i18n='{% render 'translationData' with  section.settings.title %}'>{% render 'translationText' with  section.settings.title %}</span>
    </h2>
  {% endif %}
  <div class="rte" data-rte>
    <span data-i18n='{% render 'translationData' with  section.settings.custom_html  %}'>{% render 'translationText' with  section.settings.custom_html  %}</span>
  </div>
</section>

{% schema %}
{
  "name": "Custom HTML",
  "class": "html--section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Heading",
      "default": "Custom HTML"
    },
    {
      "type": "html",
      "id": "custom_html",
      "label": "HTML",
      "default": "<p>Write or paste your HTML code here</p>"
    }
  ],
  "presets": [
    {
      "category": "Advanced",
      "name": "Custom HTML"
    }
  ]
}

{% endschema %}